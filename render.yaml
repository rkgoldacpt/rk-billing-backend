services:
  - type: web
    name: rk-billing-backend
    runtime: python
    buildCommand: ""
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000  # optional fallback
